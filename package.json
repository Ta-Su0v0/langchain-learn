{
    "name": "langchain-learn-monorepo",
    "private": true,
    "packageManager": "bun@1.3.11",
    "workspaces": [
        "apps/*",
        "packages/*",
        "tools/*"
    ],
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "test": "turbo run test",
        "typecheck": "turbo run typecheck",
        "lint": "oxfmt . && oxlint . --fix"
    },
    "devDependencies": {
        "@types/bun": "^1.3.11",
        "turbo": "^2.8.20",
        "oxfmt": "^0.42.0",
        "oxlint": "^1.57.0",
        "typescript": "6.0.2"
    },
    "engines": {
        "node": "24.x",
        "bun": ">=1.3.11"
    }
}
